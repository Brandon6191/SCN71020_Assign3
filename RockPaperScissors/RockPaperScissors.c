#include <stdio.h>
#include <string.h>

const char* RockPaperScissors(const char* player1, const char* player2) {
    //Checks for tie
    if (strcmp(player1, player2) == 0) {
        return "Tie";
    }
}

int main() {

    return 0;
}
